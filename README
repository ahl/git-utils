Repository of git utilities.

  git-pdiff	diff against the parent regardless of commits
  git-recommit	collapse multiple commit deltas
  git-review	review board integration


How to install:

 o put the utilities somewhere that's in your PATH
 o git <command> will then implicitly invoke git-<command> from your path
